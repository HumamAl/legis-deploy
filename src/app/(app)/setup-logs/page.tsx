export default function SetupLogsPage() {
  return (
    <div className="page-container">
      <h1 className="text-lg font-semibold tracking-tight">Setup Logs</h1>
      <p className="text-sm text-muted-foreground mt-1">Build log and deploy log output from npm install and npm run setup</p>
    </div>
  );
}
