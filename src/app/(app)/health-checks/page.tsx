export default function HealthChecksPage() {
  return (
    <div className="page-container">
      <h1 className="text-lg font-semibold tracking-tight">Health Checks</h1>
      <p className="text-sm text-muted-foreground mt-1">Verification suite — password gate, login, AI generation, DOCX export</p>
    </div>
  );
}
